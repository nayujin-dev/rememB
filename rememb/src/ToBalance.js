import React from "react";

const ToBalance = () => {
  const onClick=()=>{
    
  }
  return (
    <div>
      <button onClick={onClick}>밸런스 게임하기</button>
    </div>
  );
};

export default ToBalance;