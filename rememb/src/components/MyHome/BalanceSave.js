import React from "react";

const BalanceSave = () => {
  const onClick=()=>{
    // const [who,setWho]=useState
  }
  return (
    <div>
      <button onClick={onClick}>Save</button>
    </div>
  );
};

export default BalanceSave;